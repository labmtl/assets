# OS generated files
.DS_Store

# Virtual environments
venv/
.venv/

# Python cache
__pycache__/
*.py[cod]

# Local flags (if any, though we usually commit them in this repo pattern)
# processed_flags/

# Processing artifacts that shouldn't be committed if they are transient
# (But in this repo, we commit processed_media)
